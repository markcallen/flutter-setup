services:
  smoke-test-fast:
    image: ${SMOKE_SDK_IMAGE:-ghcr.io/markcallen/flutter-setup-sdk-base:flutter-stable-ubuntu24.04}
    container_name: flutter-setup-smoke-fast
    volumes:
      - .:/app
    entrypoint: ["/bin/bash", "tests/fast_smoke_test.sh"]
